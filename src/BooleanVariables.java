public class BooleanVariables {
    public static void main(String[] args) {

        // boolean flag; // variable declaration
        // flag = true; // an assignment
        // boolean testResult = false; // variable declaration and an assignment

        // relational and logical operators evaluate to a boolean value. Store the result of a comparison in a variable:
        // boolean evenFlag = (x % 2 == 0); // true if x is even
        // boolean positiveFlag = (x > 0); // true if x is positive

        // Using the flag variables as part of a conditional statement:
        // if (evenFlag) {
            // System.out.println("n was even when I checked it");
        // }

        // To check if a flag is false, we simply negate the flag:
        // if (!evenFlag) {
            // System.out.println("n was odd when I checked it");
        // }

    }
}
